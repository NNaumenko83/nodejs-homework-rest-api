const isValidFavoriteBody = require("./isValidFavoriteBody");
const isValidId = require("./isValidId");
const validateBody = require("./validateBody");

module.exports = { validateBody, isValidId, isValidFavoriteBody };
